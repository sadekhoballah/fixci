# compiled output
/dist
/node_modules

# user-uploaded files (stored on disk in dev; served via /uploads)
/uploads

# logs
logs
*.log
npm-debug.log*

# env
.env
.env.*.local

# Firebase Admin SDK service account credentials — never commit these.
# Covers both a manually-chosen name and Firebase's own default download
# naming scheme (<project-id>-firebase-adminsdk-<hash>.json).
firebase-service-account*.json
*firebase-adminsdk*.json

# IDE
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# OS
.DS_Store

# tests
/coverage
/.nyc_output
